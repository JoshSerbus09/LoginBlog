<p id="notice"><%= notice %>

<div class="show_post">
	<h1><%= @post.title %></h1>
		<%= post.message %>
		<br />
	<p>Submitted By: <%= post.name %></p>
	<br />

	<%= link_to 'Edit', edit_posts_path(post) %> | <%= link_to 'Delete', :method => :delete, :confirm => 'Are you sure?' %>

</div>